GOAL: CI/CD pipeline that deploys Snowflake objects to dev and prod schemas using Snowflake CLI.

== PHASE 1: LOCAL DEPLOY SCRIPT ==
[ ] [Claude] Consolidate the multiple deploy scripts (deploy.sh, deploy2.sh, deploy3.sh) into one:
    scripts/deploy.sh <env>  (accepts "dev" or "prod")
    - Sources environments/dev.env or environments/prod.env
    - Builds ~/.snowflake/config.toml from env vars (SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER, SNOWFLAKE_PASSWORD)
    - Runs SQL files in order: create_users, insert_users, validate, procedures
[ ] [Yong]   Test locally: ./scripts/deploy.sh dev

== PHASE 2: AZURE DEVOPS PIPELINE (SMOKE TEST) ==
[ ] [Claude] Create azure-pipelines.yml at the repo root that just echoes a message
[ ] [Yong]   Connect the GitHub repo to Azure DevOps (Pipelines > New Pipeline > GitHub)
[ ] [Yong]   Push to main and confirm the pipeline runs and the echo appears in the log

== PHASE 3: ADD SNOWFLAKE CREDENTIALS TO AZURE DEVOPS ==
[ ] [Yong]   In Azure DevOps, go to Pipelines > Library > + Variable group
    - Name it: snowflake-credentials
    - Add variables: SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER, SNOWFLAKE_PASSWORD (mark password as secret)
[ ] [Yong]   In Azure DevOps, go to Pipelines > Environments > New Environment
    - Create an environment named: production
    - Optionally add a manual approval gate for prod deploys

== PHASE 4: FULL CI/CD PIPELINE ==
[ ] [Claude] Update azure-pipelines.yml with two stages:
    Stage 1 - Deploy_Dev:
      - Runs on every push to main
      - Installs Snowflake CLI (pip install snowflake-cli)
      - Runs: scripts/deploy.sh dev
      - Uses variable group snowflake-credentials
    Stage 2 - Deploy_Prod:
      - Runs only after Deploy_Dev succeeds
      - Only on main branch
      - Uses deployment job targeting the "production" environment (enables approval gate)
      - Installs Snowflake CLI
      - Runs: scripts/deploy.sh prod
[ ] [Yong]   Push and verify full pipeline runs end-to-end in Azure DevOps logs

== NOTES ==
- dev schema  = public   (see environments/dev.env)
- prod schema = prod     (see environments/prod.env)
- Both use database: lab_db, warehouse: COMPUTE_WH
- Snowflake CLI template syntax: <%variable%> in SQL, passed via -D key=value
